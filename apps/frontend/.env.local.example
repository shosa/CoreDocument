# ============================================================================
# CoreDocument Frontend - Configurazione Sviluppo Locale
# ============================================================================
# Copia questo file come .env.local per sviluppo locale:
#   cp .env.local.example .env.local
# ============================================================================

# ----------------------------------------------------------------------------
# NEXT.JS FRONTEND
# ----------------------------------------------------------------------------
# In sviluppo locale (npm run dev):
# - Frontend gira su porta 3000
# - Next.js fa automaticamente proxy di /api -> localhost:3003/api
# - Vedi next.config.mjs per la configurazione rewrites()

# API URL (sempre /api, il proxy lo gestisce Next.js)
NEXT_PUBLIC_API_URL=/api

# Environment
NODE_ENV=development

# ----------------------------------------------------------------------------
# NOTE
# ----------------------------------------------------------------------------
# In Docker (production):
# - Frontend container porta interna 3000 -> host 3002
# - Backend container porta interna 3003 -> host 3003
# - Nginx su porta 81 fa proxy di /api -> backend:3003
